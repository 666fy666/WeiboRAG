# 核心依赖（已迁移到 pyproject.toml，使用 uv 管理）
# 此文件保留作为参考，建议使用 uv 进行依赖管理
#
# 安装依赖请使用：
#   uv pip install -e .
# 或
#   uv sync

# HTTP 客户端
requests>=2.32.0

# HTML 解析
beautifulsoup4>=4.13.0
lxml>=5.4.0

# 向量索引（根据平台选择）
# Linux x86_64: faiss-gpu>=1.7.0
# 其他平台: faiss-cpu>=1.7.0
faiss-cpu>=1.7.0
faiss-gpu>=1.7.0; sys_platform == 'linux' and platform_machine == 'x86_64'

# 深度学习框架
torch>=2.0.0
transformers>=4.30.0
modelscope>=1.0.0

# 数值计算
numpy>=1.24.0

# 检索
rank-bm25>=0.2.0

# 环境变量
python-dotenv>=1.0.0
